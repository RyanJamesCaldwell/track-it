<% provide(:title, 'Projects') %>
<h1>Projects</h1>

<%= button_to 'New Project', { :action => 'new' }, method: :get %>

<% @projects.each do |project| %>
  <p><%= link_to project.name, project %></p>
<% end %>
